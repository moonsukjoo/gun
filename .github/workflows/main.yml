name: Build Android APK from AI Studio Web

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # v3에서 v4로 업데이트

      - name: Set up Node.js
        uses: actions/setup-node@v4 # v3에서 v4로 업데이트
        with:
          node-version: 18

      - name: Install Web Dependencies
        run: |
          npm install
          npm install @capacitor/core @capacitor/cli @capacitor/android

      - name: Build Web Project (Vite)
        run: npm run build

      - name: Initialize Capacitor
        run: |
          npx cap init "GeonmyeongHRM" "com.geonmyeong.hrm" --web-dir dist

      - name: Add Android Platform
        run: |
          npx cap add android
          npx cap copy android

      - name: Set up JDK 17
        uses: actions/setup-java@v4 # v3에서 v4로 업데이트
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4 # v3에서 v4로 업데이트 (이게 핵심 에러 해결!)
        with:
          name: Geonmyeong-HRM-APK
          path: android/app/build/outputs/apk/release/app-release-unsigned.apk
